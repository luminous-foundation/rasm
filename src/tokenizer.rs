enum Token {
    NUMBER,
    TYPE,
    LPAREN,
    RPAREN,
    LCURLY,
    RCURLY,
    LSQUARE,
    RSQUARE,
    QUOTE,
    DOT,
    COMMA,
    IDENT,
}

pub fn tokenize(line: &str) -> Vec<Token> {
    
}